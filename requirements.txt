# pantryapp dependencies — direct deps pinned to versions resolved on
# Python 3.14.5 (2026-05-30). All ship prebuilt wheels for cp314, so a
# plain `pip install -r requirements.txt` needs no compiler.
#
# Install:  .venv/bin/pip install -r requirements.txt
fastapi==0.136.3
sqlmodel==0.0.38
uvicorn[standard]==0.48.0
jinja2==3.1.6
python-multipart==0.0.29
